<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shedulehome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SheduleHome">
        <activity android:name=".SingleShedule"></activity>
        <activity android:name=".singleShedule" />
        <activity android:name=".editShedule" />
        <activity android:name=".viewShedule" />
        <activity android:name=".startupScreen" />
        <activity android:name=".main_page" />
        <activity android:name=".addNewShedule" />
        <activity android:name=".toolbar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>